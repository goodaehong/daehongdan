#pragma once

// 센서 입력 전용 스레드. 1초마다 읽어서 SensorState 에 채운다.
//
// 제어 루프에서 떼어낸 이유 — 센서 읽기가 블로킹이다.
// DHT22 드라이버는 sysfs 를 읽는 순간 실제 측정을 하고(캐시 2초),
// 응답이 없으면 최대 1초까지 기다린다. 이게 제어 루프 안에 있으면
// 온습도 하나 때문에 판단·대응이 통째로 밀린다.
void sensorWorker();
